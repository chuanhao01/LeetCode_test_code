const algo = {
    recur(weights, values, maxWeight){
        boo = [];
        for(let i of values){
            boo.push(0);
        }
        function r(i, v){

        }
    }
};

module.export = algo;