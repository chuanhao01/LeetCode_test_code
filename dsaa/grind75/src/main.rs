fn main() {
    // let inputs = vec![vec!["hello", ""]];
    // let outputs = vec!["no"];
    // for (_in, out) in inputs.iter().zip(outputs.iter()) {
    //     assert_eq!(out, Solution::two_sum(_in[0], _in[1]));
    // }
}

struct Solution;

impl Solution {
    pub fn two_sum(nums: Vec<i32>, target: i32) -> Vec<i32> {
        Vec::new()
    }
}
